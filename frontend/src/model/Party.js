import Resource from "./Resource";

/**
 * A Party.
 * \o/
 */
class Party extends Resource {
    constructor(href, id) {
        super(href, id);
    }
}

export default Party;

export {
    Party
};